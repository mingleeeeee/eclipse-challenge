db:
  database: $DB_DATABASE
  host: $DB_HOST
  password: $DB_PASSWORD
  port: $DB_PORT
  user: $DB_USER
generator:
  addJsExtToImportStatement: false
  gql:
    subscription: false
migration:
  dir: src/migrations
  out: src/sasat
  table: __migrate__
